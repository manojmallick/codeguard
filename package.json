{
  "name": "codeguard-starter",
  "version": "2.0.0",
  "description": "CodeGuard — DORA Art.9 merge-request review agent. Gemini 3 + Google Cloud Agent Builder + GitLab MCP.",
  "type": "module",
  "license": "MIT",
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "eval": "node scripts/eval.js"
  },
  "dependencies": {
    "@google/genai": "^0.3.0",
    "express": "^4.19.2"
  }
}
